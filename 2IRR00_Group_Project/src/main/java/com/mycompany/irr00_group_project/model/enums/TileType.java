package com.mycompany.irr00_group_project.model.enums;

/**
 * Enum representing different types of tiles in the game.
 */
public enum TileType {
    NORMAL, START, END, KEY, DOOR_CLOSED, DOOR_OPENED, OBSTACLE
}
